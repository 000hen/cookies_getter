# 基于 Chromium 的 Cookie 获取与发送工具

这是一个简单的基于 Chromium 浏览器的 Cookie 数据获取与解密工具。
包含一个简单的远程服务器，用于接收客户端解密后的 Cookie。

> **注意**  
> 本源代码仅可用于教育目的！
> 严禁用于任何犯罪行为。

## 安装

您需要安装最新版本的 Rust 才能构建此项目。

您只需使用 `cargo run` 即可运行该程序。
